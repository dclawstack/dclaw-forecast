apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-forecast.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-forecast.name" . }}
spec:
  rules:
    - host: dclaw-forecast.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-forecast.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
